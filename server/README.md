## Server

to start the server, call:
npm start
This will run nodemon on the server file, (which restarts the server when it detects a change in the file) and call a linter before starting the server.

nodemon: https://www.npmjs.com/package/nodemon
ESLint: https://eslint.org
